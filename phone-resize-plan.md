# Historical: phone-fit pane sizing

Shipped in v0.4.0 as leased view-size overrides (`relay.allow_view_resize`,
`vmux view-size`, relay `view_cols` / `view_rows` on `surface.subscribe`).

User-facing docs: [docs/relay.md](docs/relay.md). Open residual (multi-phone
`min()` across leases) is tracked in [todo.md](todo.md).
